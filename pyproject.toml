[project]
name = "infantia"
version = "0.1.0"
description = "Privacy-first child health tracker — self-hostable, GDPR-compliant"
requires-python = ">=3.11"

[tool.ruff]
line-length = 120
target-version = "py311"
